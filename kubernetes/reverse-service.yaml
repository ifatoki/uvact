apiVersion: v1
kind: Service
metadata:
  labels:
    app: reverse
  name: reverse
spec:
  ports:
  - name: "5000"
    port: 5000
    targetPort: 5000
    nodePort: 30008
  selector:
    app: reverse
  type: NodePort
